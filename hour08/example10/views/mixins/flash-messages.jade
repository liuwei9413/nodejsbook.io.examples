mixin flash-messages(flash)
    - if (flash.waring)
        div.alert-message.warning
            p= flash.warning
    - if (flash.info)
        div(data-alert='alert').alert-message.success
            p= flash.info


